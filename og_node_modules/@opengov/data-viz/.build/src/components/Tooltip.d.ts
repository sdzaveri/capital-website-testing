import * as React from 'react';
import { ICoordinate } from '../data';
export interface ITooltipWithContent<I> extends ITooltip {
    info: I;
    moveTooltip?(x: number, y: number): void;
    showTooltip(category: string, info: I, highlightColor: string, domCoordinate: ICoordinate): void;
}
export interface ITooltip {
    boundingClientRect?: ClientRect;
    show: boolean;
    x: number;
    y: number;
    xFocalPoint: number;
    yFocalPoint: number;
    reportBoundingClientRect?(rect: ClientRect): void;
    hideTooltip?(): void;
}
export declare const Tooltip: React.SFC<ITooltip>;
