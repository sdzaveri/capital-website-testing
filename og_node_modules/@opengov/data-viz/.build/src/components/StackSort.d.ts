import * as React from 'react';
import { StackSortOrder } from '../data';
import { ISortStyle } from './Sort';
export interface IStackSortProps {
    sortOrder?: StackSortOrder;
    style: ISortStyle;
    onSort(order: StackSortOrder): void;
}
export declare const StackSort: React.SFC<IStackSortProps>;
