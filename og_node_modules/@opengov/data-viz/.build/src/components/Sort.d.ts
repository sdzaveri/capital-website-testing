import * as React from 'react';
import { SortOrder } from '../data';
import * as sortStyle from './Sort.css';
export { sortStyle };
export interface ISortStyle {
    text: string;
}
export interface ISort {
    sortOrder?: SortOrder;
    style?: ISortStyle;
    onSort(order: SortOrder): void;
}
export declare const stackOrderToText: (sortOrder: SortOrder) => "Sort Smallest to Largest" | "Sort Largest to Smallest";
export declare const Sort: React.SFC<ISort>;
