import * as React from 'react';
export interface ILegend {
    domainKeys: any[];
    x: number;
    pointSize: number;
    hoveredItem?: string;
    onItemHover(domain: string | undefined): void;
    color(domain: string): string;
}
export declare const Legend: React.SFC<ILegend>;
