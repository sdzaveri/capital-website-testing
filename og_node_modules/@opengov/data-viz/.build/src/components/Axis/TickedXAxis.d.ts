import { ScaleLinear } from 'd3-scale';
import * as React from 'react';
export interface ITickedXAxisStyle {
    axisLine?: string;
    axisTick?: string;
    text?: string;
    tick: string;
}
export interface ITickedXAxis {
    style?: ITickedXAxisStyle;
    approximateNumberOfTicks?: number;
    tickHeight?: number;
    tickLabelSpacing?: number;
    xScale: ScaleLinear<number | string, number>;
    yScale: ScaleLinear<number | string, number>;
    formatter?(domain: number | string): string;
}
export declare const TickedXAxis: React.SFC<ITickedXAxis>;
