import { ScaleBand, ScaleOrdinal } from 'd3-scale';
export declare const Scale: {
    Linear: {
        ticks: {
            byNumber: (scale: any, numberOfTicks: number) => number[];
        };
    };
    Band: {
        center: (scale: ScaleBand<any>) => (d: any) => number;
        ticks: {
            byWidth: (scale: ScaleBand<any>, width: number) => any[];
            byHeight: (scale: ScaleBand<any>, height: number) => any[];
            byMax: (scale: ScaleBand<any>, max?: number) => number[];
        };
    };
    Ordinal: {
        ticks: {
            byWidth: (scale: ScaleOrdinal<number, number>, width: number) => any[];
            byMax: (scale: ScaleOrdinal<number, number>, max?: number) => number[];
        };
    };
};
